[project]
name = "2021-11-29"
version = "0.0.0"
requires-python = "~=3.12.0"
dependencies = [
    "ampform[viz]==0.12.*",
    "ipywidgets",
    "matplotlib",
    "numpy~=1.0",
    "qrules[viz]==0.9.*",
    "tensorwaves[jax,pwa]==0.4.*"
]

[tool.uv.workspace]
